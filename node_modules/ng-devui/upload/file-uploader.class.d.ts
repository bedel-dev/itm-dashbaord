import { IUploadOptions, UploadStatus } from './file-uploader.types';
export declare class FileUploader {
    file: File;
    uploadOptions: IUploadOptions;
    private xhr;
    status: UploadStatus;
    response: any;
    percentage: number;
    constructor(file: File, uploadOptions: IUploadOptions);
    private sendCommonHandle;
    private sendErrorAndLoadHandle;
    send(uploadFiles?: any): Promise<{
        file: File;
        response: any;
    }>;
    sendMultiple(uploadFiles?: any): Promise<{
        file: File;
        response: any;
        status: UploadStatus;
    }>;
    parallelUploadFiles(fileFieldName_: any, additionalParameter: any): FormData;
    oneTimeUploadFiles(fileFieldName_: any, additionalParameter: any, uploadFiles: any): FormData;
    cancel(): void;
}
